use crate::ast1::Stmt;
// We are given a Vec<Stmt>

// Lets do something useful with it; like binding identifiers to operations
// CallExprs
// FunctionBlocks

fn parse(stmts: Vec<Stmt>) -> Vec<Stmt> {
    stmts
}